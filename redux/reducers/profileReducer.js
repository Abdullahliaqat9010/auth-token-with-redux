const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case "SET_PROFILE": {
      return state;
    }
    default: {
      return state;
    }
  }
};

export default reducer;
